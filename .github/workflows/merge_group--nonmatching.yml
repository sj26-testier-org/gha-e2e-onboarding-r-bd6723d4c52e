{
  "name": "merge_group--nonmatching",
  "on": {
    "merge_group": {
      "types": [
        "destroyed"
      ]
    }
  },
  "permissions": {},
  "jobs": {
    "probe": {
      "name": "merge_group--nonmatching",
      "runs-on": "ubuntu-latest",
      "timeout-minutes": 5,
      "steps": [
        {
          "uses": "actions/checkout@v4"
        },
        {
          "uses": "actions/setup-node@v4",
          "with": {
            "node-version": "22",
            "token": ""
          }
        },
        {
          "name": "Execute complete event probe",
          "env": {
            "LAB_CASE": "merge_group--nonmatching",
            "EVENT_FAMILY": "${{ github.event_name }}",
            "EXPR_EVENT": "${{ toJSON(github.event) }}",
            "EXPR_REF": "${{ github.ref }}",
            "EXPR_SHA": "${{ github.sha }}",
            "EXPR_ACTION": "${{ github.event.action }}"
          },
          "run": "node probe.cjs"
        }
      ]
    }
  }
}
