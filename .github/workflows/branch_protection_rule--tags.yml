{
  "name": "branch_protection_rule--tags",
  "on": {
    "branch_protection_rule": {
      "types": [
        "created"
      ],
      "tags": [
        "never-this-tag"
      ]
    }
  },
  "permissions": {},
  "jobs": {
    "probe": {
      "name": "branch_protection_rule--tags",
      "runs-on": "ubuntu-latest",
      "timeout-minutes": 5,
      "steps": [
        {
          "uses": "actions/checkout@v4"
        },
        {
          "uses": "actions/setup-node@v4",
          "with": {
            "node-version": "22",
            "token": ""
          }
        },
        {
          "name": "Execute complete event probe",
          "env": {
            "LAB_CASE": "branch_protection_rule--tags",
            "EVENT_FAMILY": "${{ github.event_name }}",
            "EXPR_EVENT": "${{ toJSON(github.event) }}",
            "EXPR_REF": "${{ github.ref }}",
            "EXPR_SHA": "${{ github.sha }}",
            "EXPR_ACTION": "${{ github.event.action }}"
          },
          "run": "node probe.cjs"
        }
      ]
    }
  }
}
